const superHeroes: string[] = [];
const times: number[] = [];

const otherHeroes: Array<string> = [];
const otherTimes: Array<number> = [];

superHeroes.push('Spider-man');
times.push(12);

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({ name: 'John Dee', isActive: true });
